# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Env (secrets)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*.local

# Capacitor native projects (created locally via `npx cap add ios|android`).
# Kept out of git for now so the repo stays light; check them in once you're
# ready to maintain native code. App icons/splash live in `resources/`.
ios/
android/
.gradle/
DerivedData/
*.xcuserstate
xcuserdata/
